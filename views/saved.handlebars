
<body class="container">
    <h1>desiring<span class="red">God</span> Saved Articles</h3>
        <div>
            <button id="backButton">HOME</button>
        </div>
        <hr>
        <div class="card-list">
            {{#each savedArticles}}
            <button class="card">
                <a href={{link}} target="_blank">{{title}}</a>
            </button>
          {{/each}}

        </div>

